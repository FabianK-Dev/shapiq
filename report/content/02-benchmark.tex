% ==================================================
% 2. Cross-method benchmark
% ==================================================
\chapter{Cross-Method Benchmark}
\label{sec:benchmark}

% The one place in the report where all three approximators and the existing shapiq
% methods are measured on the same footing. Everything here comes from one run:
%   benchmark/results/lmu_full_sweep_20260717/
% whose README records the exact command, the code revision and the machine.

\section{Setup}
\label{sec:benchmark:setup}

\begin{configtable}{Benchmark configuration. Provenance:
    \texttt{benchmark/results/lmu\_full\_sweep\_20260717/README.md}.}
    Games & TODO: 11 --- SOUM($n$=6,8,10) and the ML games IRIS(4), California(8),
        Diabetes(10), Adult(12), Correlated(60), Independent(60), NHANES(79),
        Communities(101). The ML grid follows Musco \& Witter. \\
    Value function & TODO: features masked against a single baseline row (the training
        mean); \texttt{XGBRegressor}, 100 trees, depth 4. \emph{Not} any paper's value
        function --- see Section~\ref{sec:introduction:two-questions}. \\
    Ground truth & TODO: exact enumeration for $n \le 10$; interventional TreeSHAP
        against the same baseline row above it. \\
    Budgets & TODO: $m = c \cdot n$ for $c \in \{1,2,3,5,8,12,20,30,50,75,110,160\}$. \\
    Repeats & TODO: 10 held-out predictions per game, model fixed. Median and IQR are
        over those predictions. \\
    Methods & TODO: every approximator registered in
        \texttt{shapiq.approximator.SV\_APPROXIMATORS}. \\
    Run & TODO: LMU CIP cluster, one job per prediction. \\
\end{configtable}

% TODO: one paragraph on why budgets are multiples of $n$ and not fractions of $2^n$:
% a fraction of the coalition space is not a runnable budget at $n=60$ (5\% is 5.8e16
% evaluations), and at 100\% every method is exact, so those ranks are ties rather than
% a comparison. Multiples of $n$ are the convention the papers use and apply to every game.
\lipsum[5][1-4]

\section{Results and Discussion}
\label{sec:benchmark:results}

% TODO: figures. Use plots_paper_subset/ (8 curves: the three approximators plus the
% baselines the OddSHAP paper compares against) rather than plots/ (15 curves) --- the
% full set belongs in an appendix if anywhere.
%
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=\textwidth]{figures/MSE_Communities_n101.png}
%     \caption{MSE against budget on Communities ($n=101$). Median over ten predictions,
%         band is the interquartile range. Solid: the approximators added in this project.
%         Dashed: baselines.}
%     \label{fig:benchmark:communities}
% \end{figure}

% TODO: the rank table over all comparison cells.

\lipsum[6][1-5]


% TODO. This is the deliverable's "where do the new methods win, where do they lose, and
% why --- based on what the papers predict". Points worth making, each backed by the CSV:
%
%   - Where each method wins, as a function of budget rather than averaged over it. An
%     average rank over all budgets hides that the ordering changes with $m$.
%   - Methods that decline a budget below their minimum (OddSHAP, SPEX, ProxySPEX) are
%     scored only where they ran, so their average rank is not comparable with a method
%     scored everywhere. Say so wherever an average rank appears.
%   - The small games saturate: at a budget approaching $2^n$ every method is near-exact
%     and the ranking is tie-breaking. The large games carry the comparison.
%   - Read against what the papers predict, not just as a leaderboard.
\lipsum[7][1-6]

\paragraph{Threats to validity.}

% TODO. Better to state these than to have them asked. At least:
%   - The value function is not any paper's, so this benchmark tests the methods against
%     each other, not the papers' claims.
%   - Ten predictions per game: the quartiles are a coarse envelope, not a confidence
%     interval.
%   - FourierSHAP and the fixed-budget FFD variants are in the OddSHAP paper's comparison
%     but not in shapiq, so this is the paper's baseline set intersected with what is
%     available.
%   - ShaplEIG is registered but needs an optional extra that was not installed, so it is
%     skipped everywhere.
\lipsum[8][1-4]
